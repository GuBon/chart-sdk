{
  "name": "chartsdk",
  "version": "0.1.0",
  "private": true,
  "description": "사내 임베드 차트 솔루션 모노레포 — Spring Boot 백엔드 + Next 관리콘솔 + Vanilla SDK",
  "workspaces": [
    "chart-options",
    "admin",
    "sdk"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "gen:defaults": "npm run gen -w @chartsdk/chart-options",
    "dev": "npm run dev -w @chartsdk/admin",
    "build:sdk": "npm run build -w @chartsdk/sdk",
    "build:admin": "npm run build -w @chartsdk/admin",
    "build": "npm run gen:defaults && npm run build -w @chartsdk/admin",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test:unit": "npm run test:unit --workspaces --if-present",
    "test:parity": "node scripts/verify-transform-parity.mjs",
    "test:e2e": "npm run test:e2e -w @chartsdk/admin",
    "test:e2e:real": "npm run gen:defaults && npm run build:sdk && node admin/scripts/copy-sdk.mjs && playwright test --config=admin/playwright.real.config.ts"
  }
}
