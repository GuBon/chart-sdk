{
  "name": "chartsdk",
  "version": "0.1.0",
  "private": true,
  "description": "사내 임베드 차트 솔루션 모노레포 — Spring Boot 백엔드 + Next 관리콘솔 + Vanilla SDK",
  "workspaces": [
    "chart-options",
    "admin",
    "sdk"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "gen:defaults": "npm run gen -w @chartsdk/chart-options",
    "dev": "npm run dev -w @chartsdk/admin",
    "build:sdk": "npm run build -w @chartsdk/sdk",
    "build": "npm run gen:defaults && npm run build -w @chartsdk/sdk"
  }
}
