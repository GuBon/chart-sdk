# server (Spring Boot) — 메타 DB 연결. Type A/B 공통, 값만 다르다.
DATABASE_URL=jdbc:postgresql://localhost:5432/chartsdk
DATABASE_USER=chartsdk
DATABASE_PASSWORD=changeme

# 데이터소스 비밀번호 암호화 키 (AES-GCM) — 운영에서 교체
DATASOURCE_ENC_KEY=

# admin (Next) — 백엔드 API 베이스
NEXT_PUBLIC_API_BASE=http://localhost:8080
