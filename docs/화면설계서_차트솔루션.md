# 차트 솔루션 화면설계서 v3.1 (Figma 작업용)

**문서 버전:** v3.1 — 관계 원본·조인/뷰 결과 표본 UI (2026-07-16 갱신)
**작성일:** 2026-06-11
**관련 문서:** PRD v2.6, API 계약서 v2.7, 노코드 SQL 생성규칙 v2.7
**용도:** Figma 화면 설계의 기준 문서. 프레임 구성, 영역 치수, 컴포넌트, 상태를 정의한다.

---

## 1. 설계 원칙

1. 화면 단위가 아니라 상태(state) 단위로 설계한다. 빈 상태·로딩·성공·에러를 모두 프레임으로 만든다.
2. 내비게이션은 사이드바 없이 얇은 상단 바 하나로 통일한다. 이동 목적지가 적고(목록·설정), S2 편집 화면의 가로 공간을 최대로 확보하기 위함이다.
3. S2(편집)는 자체 3분할 레이아웃을 가지므로 전역 내비가 작업 공간을 침범하지 않게 한다.
4. 임베드(S4)의 실패가 호스트 페이지를 깨뜨리지 않는다. 차트 영역 안에서만 에러를 표시한다.
5. SQL 에러는 가공 없이 그대로 노출한다(메시지·라인 위치). 사용자가 즉시 수정할 수 있게.

### 벤치마킹 기준

| 참고 대상 | 가져올 것 |
|---|---|
| Grafana 패널 편집기 | 우측 시각화 옵션 패널 — 접이식 섹션 구조, 옵션 그룹화 |
| Redash 쿼리 편집기 | 좌측 스키마 브라우저 + SQL 에디터 + 결과 표 레이아웃 |
| Metabase | 상단 바 중심 내비게이션, SQL 화면의 전체 흐름 |

---

## 2. 공통 레이아웃 / 디자인 토큰

### 2.1 기준 해상도

- 데스크톱 1440 × 900 기준으로 설계한다 (Figma 프레임: Desktop 1440).
- 사내 도구이므로 모바일 대응은 범위 외. 최소 지원 폭 1280.

### 2.2 상단 바 (전 페이지 공통, GNB)

| 속성 | 값 |
|---|---|
| 높이 | 56px 고정 |
| 좌측 | 로고 + 솔루션명 (클릭 시 S1 목록으로) |
| 우측 | 데이터소스(S5) · 토큰 관리(S7) 텍스트 링크 · (인증 도입 후) 사용자 아바타 |
| 배경 | 흰색, 하단 1px 보더 |

- S2(편집)에서는 상단 바 바로 아래에 편집 전용 헤더(차트명·저장·임베드 버튼)가 추가로 붙는다. 전역 바와 화면 헤더를 분리한다.

### 2.3 색상 토큰 (Figma Variables 권장)

| 토큰 | 용도 | 예시 값 |
|---|---|---|
| primary | 버튼, 활성 상태, 기본 차트색 | #5470C6 (ECharts 기본) |
| success | 실행 성공, 활성 뱃지 | #1D9E75 |
| danger | 에러, 삭제, 토큰 만료 | #D14343 |
| info | 로딩, 안내 | #378ADD |
| text-primary / secondary / tertiary | 본문 / 보조 / 힌트 | #1A1A1A / #666 / #999 |
| border | 카드·패널 테두리 | #E5E7EB |
| bg-page / bg-panel | 페이지 배경 / 패널 배경 | #F7F8FA / #FFFFFF |

### 2.4 공통 컴포넌트 (Figma 컴포넌트로 제작)

| 컴포넌트 | 변형(variants) |
|---|---|
| 버튼 | primary / secondary / danger, 기본 / hover / disabled / loading |
| 차트 종류 뱃지 | 막대(bar) / 선(line) / 원형(pie) / 분포(scatter) |
| 토큰 상태 뱃지 | 활성 / 회수됨 / 만료 |
| 카드 | 차트 카드 / 추가(+) 카드 |
| 모달 | 기본 / 위험(삭제·회수) |
| 입력 | 텍스트 / 셀렉트 / 컬러피커 / 토글 |
| 탭·세그먼트 | 정의 모드 탭(노코드/SQL) / 결과 탭([실행 결과]/[원본 데이터]) / 옵션 세그먼트(막대·선 등) |
| 데이터 표 | 헤더 + 행, 숫자 우측 정렬 |
| 토스트 | 성공(저장 완료) / 실패 |
| 빈 상태 | 아이콘 + 안내문 + CTA 버튼 |
| 로딩 | 스피너 + 문구 |

---

## 3. S1 · 차트 목록

### 3.1 레이아웃

```
[상단 바 56px]
[페이지 헤더: "차트 목록" + 검색 입력(이름·설명 부분일치, 240px) + (+ 새 차트) 버튼]  ← 높이 72px, 좌우 패딩 32px
[카드 그리드]                                  ← 4열(1440 기준), 거터 20px
```

| 영역 | 치수 |
|---|---|
| 콘텐츠 최대 폭 | 1200px 중앙 정렬 |
| 카드 | 폭 275px(4열 자동) × 높이 약 260px (설명 행 포함 시) |
| 카드 썸네일 | 카드 폭 × 110px, 모서리 8px |

### 3.2 카드 구성 (위→아래)

1. 썸네일 — 정적 미리보기(차트 종류 기반 일러스트 또는 저장 시 캐시 이미지). 실시간 SQL 실행 금지.
2. 차트명 (1줄 말줄임)
3. 설명 — 1줄 말줄임, text-secondary. 없으면 행 생략 (v2.2 추가)
4. 메타 행 — 종류 뱃지 + **차트 ID(#12, text-tertiary)** + 수정일 (+ 인증 도입 후: 소유자). ID는 임베드 코드의 data-chart-id와 동일 — 디버깅·대조용 (v2.2 추가)
5. 버튼 행 — [편집] [임베드] (호버 시 우상단에 삭제 아이콘 노출. 2차: 복제 아이콘 추가 — `POST /charts/{id}/duplicate`)

### 3.3 상태 (Figma 프레임 3개)

| 프레임 | 내용 |
|---|---|
| S1-기본 | 카드 그리드 + 마지막에 점선 테두리 (+새 차트) 카드 |
| S1-빈상태 | 중앙: 아이콘 + "아직 만든 차트가 없습니다" + "첫 차트를 만들어 임베드해 보세요" + [첫 차트 만들기] |
| S1-삭제확인 | 모달(폭 400px): 제목 "차트 삭제" + 본문 "○○ 차트를 삭제할까요? 이 차트를 임베드한 모든 페이지에서 차트가 더 이상 표시되지 않습니다." (토큰은 사용자 귀속이므로 삭제되지 않음) + [취소] [삭제(danger)] |

### 3.4 동작

- [편집] → S2(해당 chartId). [임베드] → S3 모달. [+새 차트] → S2 신규.
- 데이터: `GET /api/v1/charts` (API 계약 3.1).

---

## 4. S2 · 차트 편집 (핵심 화면)

### 4.1 레이아웃 (2단 헤더 + 정의 모드 탭 + 3분할)

```
[상단 바 56px]
[편집 헤더 52px: ←목록 | 차트명 (#12) | 저장 · 임베드 코드]   ← #id는 저장된 차트만(신규는 미표시)
[정의 모드 탭: ① 노코드(기본)  ② SQL(고급, 2차 — MVP에선 비활성 "준비 중")]
┌─────────┬──────────────────────┬───────────┐
│데이터소스▾│  노코드 구성 영역 (상)  │ 차트       │
│─────────│  + 생성된 SQL(접힘)    │ 미리보기(상)│
│ 관계·     │──────────────────────│───────────│
│ 컬럼 패널 │  [실행 결과]│[원본     │ 옵션(하)   │
│ (SQL탭:  │   데이터] 탭 (하)      │ 기본 440px │
│ 스키마    │                      │           │
│ 탐색기)   │                      │           │
│ 기본 320px│                      │           │
└─────────┴──────────────────────┴───────────┘
```

- 데이터소스 선택이 차트 정의의 첫 단계다. 미선택 시 탭·에디터 비활성. 셀렉트 목록은 S5에 등록된 전체 데이터소스(`GET /api/v1/datasources`).
- 데이터소스 셀렉트의 위치는 **좌측 관계·컬럼 패널 최상단** — 데이터소스 → TABLE/VIEW/MATERIALIZED VIEW → 컬럼의 수직 선택 흐름을 한 패널에 모은다(Redash 스키마 브라우저 패턴). 편집 헤더에는 두지 않는다.
- 데이터소스 변경 규칙: 구성(테이블·축·조건)이 있는 상태에서 소스를 바꾸면 **"현재 구성이 초기화됩니다" 확인 모달**을 거친다 — 기존 구성이 이전 DB의 스키마를 가리키므로 유지할 수 없다. 신규(빈 구성) 상태에서는 경고 없이 자유 변경. 차트 1개 = 데이터소스 1개(혼합 조회 미지원).
- 새 차트는 항상 노코드 탭에서 시작한다(기본 경로). SQL 탭은 2차 구현 — MVP 프레임에선 비활성 탭으로만 표기.
- **저장된 차트로 진입하면 마지막 저장 캐시를 복원한다** — 단건 `GET /charts/{id}/preview`가 ECharts option과 집계 columns/rows를 함께 반환하므로 결과 표·차트가 즉시 채워진다. 편집기 자체는 고객 DB 쿼리를 자동 실행하지 않으며, 구성을 바꾼 뒤에는 명시적 [실행]으로 새 결과를 만든다. 신규 차트는 해당 없음. 목록 batch preview는 payload 절감을 위해 rows를 싣지 않는다.
- 노코드 탭: 좌측 패널이 관계/컬럼 선택기로 동작한다. VIEW에는 `View`, MATERIALIZED VIEW에는 `Materialized View` 배지를 붙이고 갱신되지 않은 Materialized View는 `갱신 필요`로 비활성화한다. 좌측 기본 폭은 320px(조절 범위 200~480px), 우측 차트 미리보기·옵션 패널 기본 폭은 440px(조절 범위 280~560px)이다. 중앙은 구성 영역(X축/Y축+집계/조건/정렬)이다.
- SQL 탭(2차): 스키마 탐색기 + 에디터 + 결과. MVP에선 노코드의 "생성된 SQL 보기"(읽기 전용 접이식)가 SQL 가시성을 대신한다.
- 우측(차트 미리보기 + 옵션)은 두 탭 공통.

#### 노코드 구성 영역 (중앙, 노코드 탭)

| 단계 | UI | 생성 SQL 대응 |
|---|---|---|
| 원본 | TABLE·VIEW·MATERIALIZED VIEW 셀렉트 (좌 패널 연동) | FROM (base) |
| 조인 | 조인 행(관계·종류 INNER/LEFT·ON 좌=우) + **"조인 추가"** — base 다음, N개 체인. 켜면 모든 컬럼 셀렉트가 `테이블.컬럼` qualified. 기존 표본 설정은 유지한다 | `[INNER\|LEFT] JOIN ... ON ...` (생성규칙 11장). 표본 사용 시 JOIN+WHERE 결과에서 RESULT_RANDOM 후 집계 |
| X축 | 컬럼 셀렉트 + **묶기 셀렉트**(일/주/월 — X축이 날짜 타입일 때만 노출, 기본 월) | GROUP BY + 첫 컬럼 (묶기 시 DATE_TRUNC + GROUP BY 1) |
| Y축 | 컬럼 + 집계 셀렉트(합계/평균/표준편차/분산/개수/고유 개수/최소/최대/원본값), 복수 행 추가 가능. 원본값 선택 시 모든 Y축이 원본값이어야 함 | SUM(col)·STDDEV(col)·VARIANCE(col) 또는 원본 `"col"` |
| 시리즈 나누기 | 셀렉트 — **후속, 비활성 "(예정)" 표기로 자리만 확보** | (후속) GROUP BY 2차원 + 서버 피벗 |
| 조건 | [컬럼·연산자·값] 행 추가식 | WHERE |
| 정렬 | 컬럼 + 방향 | ORDER BY |
| 표본 추출 | 토글 + 방식 `자동(서버 결정) / 직접 지정` + 직접 지정 시 크기 입력(1,000~50,000행) + 행수 추정이 있을 때만 "전체 약 N행 중" + [다시 뽑기]. 조인이 있으면 `조인 결과에서 무작위 행 표본`, VIEW면 `View 조회 결과에서 무작위 행 표본` 안내를 표시한다. 실행 결과에는 방식·실측 표본 행 수·`표본 결과`를 표시한다. SUM·COUNT 열에는 `표본 합계`·`표본 개수`를 명시하고, AVG·STDDEV·VARIANCE는 가능한 경우 "95% 신뢰수준 · 오차 약 ±X%"를 표시한다. 원본값 모드에서만 비활성화 | 물리 테이블은 INDEX_RANDOM/SYSTEM/FULL_SCAN, VIEW·조인은 RESULT_RANDOM. SUM·COUNT는 외삽하지 않으며 sampling v6를 캐시·API·SDK까지 보존 |

- 하단에 "생성된 SQL 보기" 접이식 영역 — **기본 접힘**. 헤더 클릭 시 펼쳐져 노코드가 만든 SQL(읽기 전용)을 확인. 접혀 있어도 언제든 펼칠 수 있으므로 블랙박스 방지 원칙(생성규칙 1장)은 유지된다.
- 원본값 모드(`agg:"none"`)는 모든 그래프에서 선택 가능하다. 막대/선은 X/Y 행 값을 그대로 그리고, 원형은 name/value 행 값을 그대로 그린다. 이때 생성 SQL에는 GROUP BY가 없다.
- 기준 관계 선택 즉시 하단 [원본 데이터] 탭에 schema preview 원본 행이 표시된다(최대 1,000행 스크롤 — 4.2 사양 참조). 이후 JOIN·WHERE 구성이 바뀌면 원본 결과를 무효화하고, 사용자가 [원본 데이터] 탭을 열 때 조건 적용 raw를 지연 조회한다. [실행] 버튼은 집계 쿼리만 한 번 호출한다.
- SQL 탭으로 전환: 생성 SQL이 에디터에 로드(단방향). SQL 수정 시 "노코드 구성이 폐기됩니다" 경고.

### 4.2 스키마 탐색기 (좌)

- 최상단: **데이터소스 셀렉트** (4.1 위치 규칙) — 변경 시 소스변경확인 규칙 적용
- 상단: 검색 입력 + 정렬/필터 메뉴. 검색은 현재 페이지가 아니라 전체 관계·컬럼을 대상으로 하며 검색어가 바뀌면 1페이지로 돌아간다.
- 정렬: 스키마·테이블 기본순, 이름 오름차순, 이름 내림차순. 필터: 데이터소스에 사용자 스키마가 2개 이상 있을 때만 스키마 선택 항목을 노출한다.
- 트리: 관계(펼침, `View`/`Materialized View` 배지) → 컬럼(이름 + 타입 작은 글씨). 탐색 목록은 클라이언트에서 페이지당 50개씩 표시하며 이전/다음과 현재 범위를 제공한다. `populated:false` Materialized View는 비활성화하고 `갱신 필요`를 표시한다.
- 빌더의 컬럼 타입 판정과 저장된 구성 복원에는 페이지로 잘린 목록이 아니라 API가 반환한 전체 스키마 원본을 사용한다. 페이지네이션은 탐색 UI 표시량만 제한한다.
- 동작: **관계 클릭 → 즉시 하단 [원본 데이터] 탭에 schema preview 표시** (`SELECT * … LIMIT 1000`, 집계 실행과 별개) / (SQL 탭, 2차) 컬럼 클릭 → SQL 에디터 커서 위치에 이름 삽입
- 하단 영역은 **탭 2개**다: **[실행 결과]**(집계) / **[원본 데이터]**(raw, 세로 스크롤). [실행]하면 실행 결과 탭으로 전환하고 집계 요청만 보낸다. 구성에 맞춘 raw는 사용자가 [원본 데이터] 탭을 열 때 필요한 경우 한 번 조회한다. 구성 변경·재실행 시 이전 raw를 폐기하므로 집계와 상세 행이 어긋나지 않는다.
- 표본 실행 결과는 일반 열 오른쪽에 `표본 입력 행` 열을 추가해 그룹별 `sampleCount`를 표시한다. 상단의 `N행`은 결과 그룹 수이고, 표본 배지의 실제 N행은 집계에 입력된 전체 표본 행 수이므로 서로 다른 값이다. STDDEV/VARIANCE의 95% 구간은 정규성 가정을 명시하고, 유효 표본 30개 미만 그룹은 구간에서 제외한다. MIN/MAX와 COUNT DISTINCT도 각각 별도 정확도 경고를 표시한다.
- [원본 데이터] 탭 사양: 노코드의 조건(WHERE)이 적용된 원본 행 = "이 차트가 집계한 바로 그 데이터". 최대 1,000행(시스템 행 제한과 동일)을 로드하고 세로 스크롤, 초과 시 "1,000행까지 표시" 안내(truncated). 집계 쿼리는 여전히 자동 실행하지 않는다 — 명시적 [실행]만.
- 데이터: `GET /api/v1/schema/tables`, `GET /api/v1/schema/tables/{name}/preview` (경로명은 하위 호환을 위해 tables 유지, 응답은 관계 종류 포함)
- `mc_` 접두사 관계는 표시하지 않는다.

### 4.3 SQL 에디터 + 실행 (중) — SQL 탭 · 2차

- 에디터 하단 우측: [실행 ▶] 버튼 + 단축키 표기 (Ctrl+Enter)
- 실행 중: 버튼이 로딩 상태로, 결과 영역에 "쿼리 실행 중... (타임아웃 10초)"
- 성공: 결과 표 + 우측 상단에 "3행 · 42ms" 메타 표시
- 에러: 에디터 테두리 danger + 결과 영역에 에러 박스(메시지 + LINE 위치). 차트 미리보기는 갱신하지 않고 이전 상태 유지.
- 데이터: `POST /api/v1/query/run` (API 계약 2).

### 4.4 시각화 옵션 패널 (우) — Grafana식 접이식 섹션

패널 최상단에 옵션 검색창 자리를 둔다(MVP에선 동작 미구현, 프레임에만 배치). 옵션 정의는 코드의 **단일 레지스트리(`chart-options/optionRegistry.ts` — `OptionDef[]`)** 로 관리하고 패널·서버 변환기·`options` JSONB가 모두 이 한 목록을 공유한다. 옵션 추가 = 정의 객체 한 줄.

#### 옵션 3존(zone) 구조

옵션을 "전역/차트별" 2분법이 아니라 **3존**으로 나눈다. 존이 곧 패널 배치이자 전환 시 유지/초기화 정책이다(4.4 전환 규칙과 1:1).

| 존 | 의미 | `appliesTo` | 대분류 전환 시 |
|---|---|---|---|
| **Zone 1 · 공통** | 모든 대분류 공통 | bar·line·pie·scatter | **유지** |
| **Zone 2 · 좌표/축** | 직교 차트 전용 | bar·line·scatter | 직교끼리 **유지** / 원형 전환 시 **숨김+보존**(복귀 시 복원) |
| **Zone 3 · 대분류 전용** | 한 대분류 고유 | 해당 1개 | **초기화**(+토스트·실행취소) |

각 옵션에는 시급도 `tier`도 함께 단다(레지스트리 필드). **T1 = MVP 기본 품질**(없으면 차트가 어설픔), T2 = 자주, T3 = 고급. 존(구조)과 tier(시급도)는 직교 축이다.

MVP 옵션 구성 (각 섹션은 접기/펼치기):

**Zone 1 · 공통** (모든 대분류)

| 섹션 | 옵션 | 컨트롤 | ECharts 매핑 / 비고 | tier |
|---|---|---|---|---|
| 기본 | 대분류 | 아이콘 그리드 **4열/한 줄, 인라인 래핑** (막대·선·원형·분포 활성 / 이후 차트 잠금 "후속" · 확장 규칙은 표 아래 주) | `series.type` (= `chart_type` 컬럼) | T1 |
| | 중분류(variant) | 세그먼트 — **대분류 종속, 선택 시 목록 교체** (막대: 기본/누적/그룹/가로 · 선: 기본/곡선/영역/누적영역/계단 · 원형: 파이/도넛/로즈 · 분포: 산점도/버블) | `options.variant` → 변환기가 type 교체·stack·areaStyle 등으로 해석 | T1 |
| | 차트 제목 / 위치 | 텍스트 + 세그먼트 2개(가로 좌/중앙/우 × 세로 상/하) | `title.text` / `title.left` / `title.top` | T1 |
| | 설명 | 텍스트영역 (2줄, 선택) | `mc_chart.description` (option 미반영, S1 카드용) | T1 |
| 색상 | 색 모드 / 팔레트 / 개별 색 | 세그먼트(팔레트/개별) · 프리셋 · 항목별 컬러 피커(실행 후 활성) | `color[]` / `itemStyle.color`·시리즈 `color` (막대=항목/시리즈, 선=시리즈, 원형=조각) | T1 |
| 범례 | 표시 / 위치 / 스크롤 | 토글 · 세그먼트(상/하/좌/우) · 좌·우 전용 스크롤 토글 | `legend.show` / `legend.top·bottom·left·right`(좌·우 → `orient:vertical`) / 상·하는 한 줄 보장을 위해 `legend.type:'scroll'` 자동, 좌·우는 토글로 적용 | T1 / T2 |
| 툴팁 | 트리거 / 값 포맷 / 축 지시선 | 세그먼트(항목/축) · 셀렉트 · 세그먼트(선/음영/십자) | `tooltip.trigger` / `valueFormatter` / `axisPointer.type`(축 트리거 시) | T1 |
| 계열 | 데이터 라벨 / 라벨 위치 / 정렬 | 토글 · 셀렉트 · 셀렉트(원본/오름/내림) | `series.label.show` / `label.position` / 서버 rows 정렬 | T1 / T2 |
| 데이터 갱신 | 갱신 모드 / 주기 / 지금 갱신 / 기준시각 표시 | 세그먼트(실시간/주기/수동) · 셀렉트(15분/1시간/6시간/24시간) · 버튼 · 토글 | `refresh_mode` / `cache_ttl_seconds` 컬럼 · `POST /charts/{id}/refresh`(2차) · `showComputedAt` | T1 / T2 |

> **대분류 선택기 확장 규칙**: 인라인 래핑 아이콘 그리드를 쓴다 — **한 줄 4개**, MVP 4종(막대·선·원형·분포)은 정확히 1줄. 차트 종류가 늘면 다음 줄로 래핑하고, **8~10개를 넘으면 카테고리 접이식 서브그룹**(기본 / 분포·관계 / 지도 / 기타)으로 묶는다. 팝오버·순수 텍스트 드롭다운은 채택하지 않는다(차트는 모양으로 인지 — 항상 보이는 인라인 아이콘이 펼쳐야 보이는 방식보다 빠름). 상단 옵션 검색창이 종류 필터를 겸한다. 렌더러가 `optionRegistry` 대분류 `choices` 개수로 분기하므로 스키마·DB 변경은 없다(차트 종류 추가 = 레지스트리 한 줄).

**Zone 2 · 좌표/축** (막대·선·분포 — 원형에선 숨김+보존)

| 섹션 | 옵션 | 컨트롤 | ECharts 매핑 / 비고 | tier |
|---|---|---|---|---|
| 여백 | 라벨 잘림 방지 / 여백 프리셋 | 토글 · 셀렉트(좁게/보통/넓게) | `grid.containLabel` / `grid` 여백 | **T1** / T2 |
| X축 | 제목 / 라벨 회전 / 세로 격자선 / 범위·스케일 | 텍스트 · 슬라이더 0~90° · 토글 · (분포 전용) | `xAxis.name` / `axisLabel.rotate` / `splitLine.show` / `min·max·type`(숫자 X만, `showIf`) | T1 |
| Y축 | 제목 / 단위 / 라벨 포맷 / 범위(자동·수동) / 스케일 / 가로 격자선 / **2번째 Y축** | 텍스트 · 텍스트 · 셀렉트(천단위·소수점·%) · 세그먼트+입력 · 세그먼트(선형/로그) · 토글 · 토글 | `yAxis.name` / `axisLabel.formatter`(unit+format 결합) / `min·max` / `type:value\|log` / `splitLine.show` / 2번째 `yAxis`+`series.yAxisIndex` | T1 |

**Zone 3 · 대분류 전용**

| 대분류 | 섹션 옵션 | 컨트롤 | ECharts 매핑 / 비고 | tier |
|---|---|---|---|---|
| 막대 | 막대 너비 / 간격 / 둥근 모서리 / 100% 정규화 / 배경 막대 | 슬라이더 · 슬라이더 · 슬라이더 · 토글 · 토글 | `barWidth` / `barGap` / `itemStyle.borderRadius` / 누적 정규화(`showIf` variant=누적) / `showBackground` | T1 / T2 |
| 선 | 선 굵기 / 종류 / 점 표시·크기 / 결측값 연결 / 영역 투명도 | 슬라이더 · 세그먼트(실선/파선/점선) · 토글+슬라이더 · 토글 · 슬라이더 | `lineStyle.width` / `lineStyle.type` / `showSymbol`·`symbolSize` / `connectNulls` / `areaStyle.opacity`(영역 variant) | T1 / T2 |
| 원형 | 도넛 두께 / 라벨 위치 / 시작 각도 / 최소 표시 각 | 슬라이더(도넛 variant) · 세그먼트(바깥/안쪽/중앙) · 슬라이더 · 입력 | `radius`(도넛) / `label.position` / `startAngle` / `minAngle` | T1 / T2 |
| 분포 | 점 모양 / 점 크기 / 버블 크기 기준 컬럼 | 셀렉트 · 슬라이더 · 컬럼 셀렉트(버블 variant·실행 후) | `symbol` / `symbolSize` / `symbolSize` 값 인코딩 | T1 / T3 |

확장 예약(후속, 접힌 상태로 표기만): markLine/markPoint(평균·목표선), dataZoom(줌·슬라이더), toolbox(이미지 저장·종류 전환), visualMap(조건부 서식·임계값 색), 영역 그라데이션, 0 기준 해제, 시간축(`xAxis.type:'time'`). (곡선·영역·누적·계단은 중분류 variant로 흡수)

#### 차트 종류 전환 규칙 (비파괴)

전환 정책은 옵션의 **존(zone)** 으로 일괄 결정된다 — "어떤 옵션을 지울까"를 개별 판단하지 않고 존 태그로 처리한다(레지스트리 `switchMajor()`).

- 데이터 구성(테이블·X·Y·조건·정렬)은 어떤 전환에도 초기화하지 않는다.
- 같은 대분류 내 중분류 전환(예: 기본 막대 → 누적): 옵션 전부 유지 — 즉시 재렌더만.
- 대분류 간 전환(예: 막대 → 원형):
  - **Zone 1(공통)** 은 유지.
  - **Zone 2(축)** 는 직교끼리(막대↔선↔분포) 유지, 원형으로/에서 전환 시 숨김+보존(복귀 시 복원).
  - **Zone 3(대분류 전용)** 은 즉시 초기화 + 토스트 "막대 전용 설정이 초기화되었습니다 [실행 취소]" (`switchMajor()`의 `removedKeys` 사용).
- 비호환 데이터는 무시+안내: 원형 = 첫 번째 Y 시리즈만 사용(나머지 폼에 회색 보존, 복귀 시 복원) / 분포 = 숫자 X 필요(비호환이면 X만 재선택 요구, 나머지 유지).
- 옵션 정의의 `appliesTo` + `showIf` 메타로 패널이 대분류별·조건별 옵션 노출을 필터링한다(`visibleDefs()`).

#### 색상 — 개별 색 지정 설계

- 팔레트 모드(기본): 색 배열을 순서대로 적용. 카테고리가 바뀌어도 깨지지 않음.
- 개별 지정 모드: SQL 실행 성공 후, 결과의 카테고리/시리즈가 행으로 나열되고 각 행에 컬러 피커. 실행 전에는 비활성("실행 후 지정 가능" 안내).
- 저장 형태(`options` JSONB): `colorMode`, `palette[]`, `colorMap{항목명: 색}`.
- 매핑 규칙: 항목이 `colorMap`에 있으면 그 색, 없으면(SQL 변경으로 새 항목 등장 시) 팔레트 순서로 자동 적용. 차트가 깨지지 않게 한다.

#### 데이터 형태 컨벤션 (서버 변환기 규칙)

- 첫 번째 컬럼 = X축(카테고리), 나머지 숫자 컬럼 = 각각 하나의 시리즈.
- 시리즈가 2개 이상이면 다중 막대/다중 선이 되고, 시리즈별 색·범례가 자동 구성된다.
- 전용 형태: 상자수염은 카테고리별 값의 5수 요약, 히트맵은 첫 컬럼×값 컬럼 매트릭스, 지도는 지역명+값, 지도 포인트는 경도+위도(+크기값)로 변환한다.
- 제목·범례가 같은 상·하 모서리에 있으면 두 줄로 쌓고 grid 여백을 점유 높이만큼 늘린다. 긴 제목의 폭·말줄임은 실제 미리보기 컨테이너 폭으로 계산하며 resize 때 갱신한다.

- 옵션 변경 → 차트 미리보기 즉시 갱신 (저장 전에도 반영).
- 저장 데이터: `chart_type` 컬럼 + `options` JSONB. 옵션 추가 시 스키마 변경 없음.

### 4.5 상태 (노코드 MVP — Figma 프레임 6개 + 모달 1)

| 프레임 | 내용 |
|---|---|
| S2-신규 | 데이터소스 미선택. 폼·좌측 트리 딤 + 안내 배너. 데이터소스 셀렉트 펼침 상태(등록 DB 목록 + 연결 상태) 포함. 우측 차트·옵션 비활성 + "실행 시 활성화" 안내 |
| S2-구성중 | 소스·테이블 선택됨([원본 데이터] 탭 활성, 스크롤 테이블 자동 표시), 폼 구성 진행, 실행 전 — 차트·옵션 딤. 날짜 X축 + 묶기(월) 시나리오 |
| S2-로딩 | 실행 버튼 로딩 + 결과 영역 스피너 "쿼리 실행 중... (타임아웃 10초)" |
| S2-성공 | [실행 결과] 탭 활성("N행 · Nms" + 집계 표) + [원본 데이터] 탭 전환 가능 + 생성된 SQL 보기(기본 접힘) + 차트 + 옵션 전부 활성 |
| S2-에러 | 검증 400 에러 박스(danger, 친화적 문구 — DB 에러 아님) + 문제 입력 강조. 차트는 이전 상태 유지 |
| S2-저장 | 완료 토스트 "저장되었습니다" |
| S2-소스변경확인 | 모달: "데이터소스를 변경할까요?" + 구성 초기화 경고 + [취소] [변경] (4.1 변경 규칙) |
| S2-이탈확인 | 모달: "저장되지 않은 변경이 있습니다" + "이대로 나가면 변경 내용이 사라집니다" + [계속 편집] [저장 안 함] [저장 후 나가기] |

- 저장 조건(PRD 7.3): 차트명 + 소스·테이블·X·Y 최소 구성 + 검증 통과. 저장 = 서버 실행 + 캐시 시드 — 실행 실패 시 저장 실패.

---

## 5. S3 · 임베드 코드 (모달)

### 5.1 레이아웃 (모달 폭 520px)

1. 제목: "임베드 코드" + 닫기 ✕
2. 사용자 토큰 선택: 셀렉트 (S7에서 발급된 활성 토큰 목록. 없으면 "토큰 관리에서 발급" 링크)
3. 코드 블록 (선택한 토큰이 끼워진 스니펫) + [복사]

### 5.2 코드 블록 내용

```html
<div data-chart-id="12" data-auth-token="{선택한 사용자 토큰}"></div>
<script src="https://.../sdk.js"
        data-api-base="https://.../api서버"></script>
```

- 복사한 코드만 호스트 HTML에 붙이면 별도 초기화 없이 렌더되어야 한다. `script.src`는 SDK 자산 위치, `data-api-base`는 차트 데이터·지도 자산을 읽을 Spring API 위치이며 서로 다른 출처일 수 있다.
- Admin dev/build는 SDK 번들을 `admin/public/sdk.js`로 자동 배포한다. 운영에서 CDN을 쓰면 `NEXT_PUBLIC_SDK_SRC`, API 위치는 `NEXT_PUBLIC_API_BASE`로 주입한다.

### 5.3 상태 (Figma 프레임 2개)

| 프레임 | 내용 |
|---|---|
| S3-기본 | 토큰 선택 + 스니펫 + 복사 (토스트 "복사되었습니다") |
| S3-토큰없음 | "활성 토큰이 없습니다" + [토큰 관리로 이동] |

- 토큰 발급/회수는 S7로 이동했다. 본 모달은 코드 조립과 복사만 담당한다.

## 6. S4 · 임베드 렌더 (외부 페이지, 런타임)

우리가 만드는 페이지는 아니지만 sdk.js의 표시 사양으로 프레임을 만든다. 차트 div 영역(예: 600×360) 안에서만 표현한다.

### 6.1 상태 (Figma 프레임 3개)

| 프레임 | 내용 |
|---|---|
| S4-로딩 | 중앙 스피너 + "차트 불러오는 중" (text-tertiary) |
| S4-정상 | ECharts 차트 + 하단 "데이터 기준 {시각}" 캡션(text-tertiary — 캐시 모드일 때, 옵션으로 끔 가능) |
| S4-에러 | 중앙 자물쇠/경고 아이콘 + "차트를 표시할 수 없습니다" + 보조문 "(토큰 만료 또는 무효)" — 호스트 페이지는 영향 없음 |

- 에러 원인별 문구는 동일하게 유지한다(만료/회수/서명오류를 외부에 구분 노출하지 않음 — 정보 최소화).

---

## 7. S5 · 데이터소스 관리 (코어로 승격)

상단 바에서 진입. 등록된 데이터소스 목록 + 추가/수정/삭제.

### 7.1 레이아웃

- 목록: 테이블형 (이름 · 호스트:포트/DB명 · 상태 점 · 연결 테스트 버튼 · 수정/삭제)
- [+ 데이터소스 추가] → 모달: 이름 / 호스트 / 포트 / DB명 / 계정 / 비밀번호 + [연결 테스트] + 안내문 "읽기 전용 계정을 권장합니다"

### 7.2 상태 (Figma 프레임 4개)

| 프레임 | 내용 |
|---|---|
| S5-목록 | 등록된 소스 테이블 |
| S5-빈상태 | "데이터소스를 먼저 등록하세요" + CTA (최초 진입 동선) |
| S5-추가모달 | 입력 폼 + 연결 테스트 성공/실패 인라인 표시 |
| S5-삭제확인 | "이 소스를 사용하는 차트 N개가 동작을 멈춥니다" 경고 |

- 비밀번호는 저장 후 다시 표시하지 않는다(변경만 가능).
- 수정 = 추가 모달 재사용: 기존 값 프리필, 비밀번호는 빈칸("변경할 때만 입력") — 입력 시에만 갱신(`PUT`의 dbPassword 선택 전달 규칙).
- 고급 설정(접이식): 커넥션 상한(max_pool_size, 기본 5) — 운영 DB 보호용.
- 데이터: mc_datasource. 사용 중인 소스 삭제 시 차트 영향 경고 필수.

## 8. S6 · 로그인 (임시 골격) / S7 · 토큰 관리 (사용자별)

### 8.1 S6 · 임시 로그인

인증 구현은 개발 중 후순위로 미뤄질 수 있다. 화면은 골격만: 중앙 카드(400px) — ID / PW / [로그인]. 추후 SSO 전환 시 이 카드가 "사내 계정으로 로그인" 버튼 하나로 교체된다(임베드 구조 불변).

| 상태 | 내용 |
|---|---|
| 기본 | ID/PW 폼 |
| 실패 | "로그인 정보를 확인하세요" 인라인 |
| (개발 중) 우회 | 인증 미구현 단계에선 본 화면을 건너뛰고 Admin 직행 가능 |

### 8.2 S7 · 토큰 관리 (사용자 임베드 토큰)

토큰이 차트가 아니라 사용자에 귀속되므로, 토큰 관리가 차트 모달(S3)에서 독립 화면으로 분리된다.

- 레이아웃: 사용자별 토큰 목록 (사용자 · 토큰 미리보기 · 만료 · 상태 · 발급일) + [발급] [회수]
- S3(임베드 모달)은 이제 "코드 조립기"가 된다: 차트 선택은 끝났고, 사용자 토큰을 골라 스니펫에 끼워 복사.
- 전체 사용자 뷰는 인증 전 과도기용(운영자가 수동으로 사용자 생성·토큰 대리 발급). **SSO 도입 후 S7은 "내 토큰" 셀프서비스로 축소** — 본인 토큰만 조회·발급·회수하며 사용자 셀렉트는 제거된다. 화면 구조는 동일, 데이터 범위만 `GET /api/v1/users/{me}/tokens`로 변경.

| 상태 | 내용 |
|---|---|
| S7-목록 | 토큰 테이블 |
| S7-발급 | 사용자 선택 + 만료 입력 → 발급. 셀렉트 하단에 **"+ 새 사용자 만들기"** 인라인(username·표시명 2칸 → `POST /api/v1/users`) — 인증 구현 전 수동 사용자 생성의 유일한 화면 동선 |
| S7-회수확인 | "이 사용자의 모든 임베드 차트가 표시되지 않게 됩니다" 경고 |

### 8.3 토큰 계층 (변경 후)

| 토큰 | 발급 | 내용 | 용도 |
|---|---|---|---|
| 로그인 세션 | 인증 구현체(임시→SSO) | 사용자 세션 | Admin 사용 |
| 사용자 임베드 토큰 | S7에서 발급 | {userId, exp, v} | 모든 차트 임베드 인증 |

## 9. Figma 프레임 체크리스트

**공통 (먼저)**
- [ ] 디자인 토큰 / 공통 컴포넌트 / 상단 바

**S1 차트 목록 — 3프레임**: 기본 / 빈상태 / 삭제확인
**S2 차트 편집 — MVP 6프레임 + 모달 2 (최우선)**: 노코드-신규(데이터소스 미선택·셀렉트 펼침 포함) / 노코드-구성중 / 로딩 / 성공 / 에러(검증 400, DB에러 아님) / 저장 / 소스변경확인 모달 / 이탈확인 모달
**S2 SQL 탭 — 2차 3프레임 (회색 보류)**: SQL-작성 / SQL-DB에러 / 전환경고모달
**S3 임베드 모달 — 2프레임**: 기본 / 토큰없음
**S4 임베드 렌더 — 3프레임**: 로딩 / 정상 / 에러
**S5 데이터소스 관리 — 4프레임**: 목록 / 빈상태 / 추가모달 / 삭제확인
**S6 로그인 — 2프레임 (골격)**: 기본 / 실패
**S7 토큰 관리 — 3프레임**: 목록 / 발급 / 회수확인

MVP 23프레임 + 2차 3프레임 + 공통 컴포넌트. (S6은 골격)

## 10. 화면 흐름도

```
S6 로그인(임시/우회) ──→ S1 목록 ──[+새 차트]──→ S2 (데이터소스 선택 → 노코드|SQL 탭)
                          │ └─[편집]→ S2 / [임베드]→ S3 / [삭제]→ 삭제확인
S2 ──[임베드 코드]──→ S3 (사용자 토큰 선택 + 복사) ──→ 외부 페이지 ──→ S4 렌더
상단바: 로고→S1 · 데이터소스→S5 · 토큰→S7
사전 준비: S5에서 데이터소스 등록, S7에서 사용자 토큰 발급
```
 
---

## 부록. 현재 구현 반영 - S1/S2 보강

### S1 차트 목록

- 목록은 무한 스크롤이 아니라 페이지네이션으로 동작한다.
- 기본 `pageSize`는 12이며, 필터/정렬/검색 조건이 바뀌면 `page`는 1로 초기화한다.
- 정렬/필터링은 백엔드 `GET /api/v1/charts`에서 처리한다.
- 카드 미리보기는 더미 미니차트가 아니라 서버 preview option을 실제 ECharts로 렌더링한다.
- 현재 페이지의 미리보기는 `GET /api/v1/charts/previews?ids=...` batch API로 한 번에 가져온다.
- 새 차트 만들기 카드는 필터가 없고 1페이지일 때만 목록 카드 그리드 끝에 표시한다.

### S2 차트 편집 - 색상

- 색상 섹션의 팔레트 블록은 클릭 가능한 swatch로 표시한다.
- 선택된 swatch는 border/ring으로 구분한다.
- swatch 아래에는 선택 색상의 color input과 RGB 숫자 입력을 제공한다.
- RGB 값은 0~255 범위로 clamp하고, 변경 즉시 `options.palette[]`에 반영한다.
- `options.palette[]` 변경은 기존 옵션 preview 재조립 흐름을 그대로 타므로, SQL 재실행 없이 차트 미리보기가 갱신된다.
- 각 입력/select에는 `id` 또는 `name`을 부여해 브라우저 form field 경고를 방지한다.
