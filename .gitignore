# dependencies
node_modules/

# builds
dist/
admin/.next/
admin/.next-e2e/
admin/.next-*/
admin/out/
sdk/dist/
admin/public/sdk.js
admin/public/sdk.js.map
admin/public/fonts/

# generated SSOT artifact (run `npm run gen:defaults`)
chart-options/defaults.json

# java / gradle
server/build/
server/.gradle/
server/bin/

# typescript incremental
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example

# playwright
admin/test-results/
admin/playwright-report/
admin/blob-report/
admin/.playwright/
.playwright-cli/
sdk/test-results/

# misc
*.log
.DS_Store
.tmp_*

# playwright real-run screenshots (스크립트 .cjs/.ps1 는 보존)
output/**/*.png
output/playwright/

# Large local public-data imports
/AL_D010_*/

# 개인 작업 기록용 문서 — Flyway 마이그레이션 SQL(docs/V*.sql, afterMigrate__*.sql)은 빌드가
# 실제로 읽으므로 계속 추적한다. .md/.html 문서만 로컬 전용으로 두고 원격에는 올리지 않는다.
docs/**/*.md
docs/**/*.html
